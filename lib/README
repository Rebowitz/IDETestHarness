This directory contains .jar files that are required to be installed in the
web container's lib directory in order for this application to work.  It
currently contains the following .jar files from the indicated distributions:

jaxws-ri_2.1.3
    activation.jar
    FastInfoset.jar
    http.jar
    jaxb-api.jar
    jaxb-impl.jar
    jaxb-xjc.jar
    jaxws-api.jar
    jaxws-rt.jar
    jaxws-tools.jar
    jsr173_api.jar
    jsr181-api.jar
    jsr250-api.jar
    mimepull.jar
    resolver.jar
    saaj-api.jar
    saaj-impl.jar
    sjsxp.jar
    stax-ex.jar
    streambuffer.jar
